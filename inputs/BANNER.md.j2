# X MRR Banner Generation Specification

Generate a complete X profile banner containing the real business data below.

The output must be the FINAL banner layout (text, status card, progress bar).
App icon markers are solid pure red ({{ icon_marker_hex }}) squares — real logos replace them afterward.
Do not leave other placeholders visible.
All supplied values must be rendered accurately.

## Canvas

- Width: {{ banner.width }} px
- Height: {{ banner.height }} px
- Aspect ratio: {{ banner.aspect_ratio }}
- Platform: X profile banner
- Compose the full layout for a wide 3:1 banner ({{ banner.width }}×{{ banner.height }}).
- Keep all text and UI inside a safe margin (~4% inset from every edge).
- Do not place critical text in the extreme top or bottom strips.

## Challenge

- Challenge headline: {{ challenge.headline }}
- Start date: {{ challenge.start_date }}
- Deadline: {{ challenge.deadline }}
- Current period: {{ challenge.current_period }}
- Total periods: {{ challenge.total_periods }}
- Target MRR: {{ challenge.target_mrr_formatted }}
- Current MRR: {{ revenue.current_mrr_formatted }}
- Progress toward target: {{ revenue.target_progress_percent }}%

## Apps

{% for app in apps %}
### {{ app.name }}

- Current MRR: {{ app.current_mrr_formatted }}
- Apple App Store MRR: {{ app.apple_mrr_formatted }}
- Google Play MRR: {{ app.google_mrr_formatted }}

{% endfor %}

## Text to Render

Render the following text clearly and exactly.

### Top Label

{{ content.top_label }}

### Main Headline

{{ content.headline }}

### Subheadline

{{ content.subheadline }}

### Period

{{ content.period_label }}

### Current MRR

{{ content.revenue_label }}

{% if apps %}
### Apps (render beside each red marker)

{% for app in apps %}
- {{ app.name }} — total {{ app.current_mrr_formatted }} | Apple {{ app.apple_mrr_formatted }} | Google {{ app.google_mrr_formatted }}
{% endfor %}
{% endif %}

## Visual Direction

- Mood: {{ theme.mood }}
- Style: {{ theme.style }}
- Color mode: {{ theme.color_mode }}
- Background: {{ theme.background_color }}
- Primary color: {{ theme.primary_color }}
- Accent color: {{ theme.accent_color }}
- Text color: {{ theme.text_color }}
- Chart color: {{ theme.chart_color }}

Create a premium dark indie-founder / technology aesthetic.

Use:
- deep charcoal background
- subtle technical grid
- soft gradients
- faint flowing curves
- partial geometric arcs
- restrained glow
- subtle dotted accents
- strong typography
- generous negative space

Avoid:
- excessive cyberpunk styling
- excessive neon
- busy patterns
- photographic imagery
- any pure red / {{ icon_marker_hex }} except the icon markers

## Composition

Follow this structure:

### Left / Main Area

Show:
- top label
- main headline
- subheadline
{% if apps %}
- apps label: {{ content.apps_label }}
- for each app: red icon marker + name + per-store MRR (see Red Icon Markers)
{% endif %}

The main headline should be the dominant visual element.

{% if apps %}
### Red Icon Markers

Real app icons are composited programmatically AFTER generation by detecting these markers.

Draw exactly {{ apps|length }} solid rounded squares filled with pure {{ icon_marker_hex }} (RGB 255, 0, 0), left → right:

{% for app in apps %}
#### Marker {{ loop.index }} — {{ app.name }}

Beside this marker, render exactly:

- App name: {{ app.name }}
- Total MRR: {{ app.current_mrr_formatted }}
- Apple: {{ app.apple_mrr_formatted }}
- Google: {{ app.google_mrr_formatted }}

Layout for this app (compact, readable):
- red marker on the left
- app name immediately to the right of the marker
- under or beside the name, show store split as: `Apple {{ app.apple_mrr_formatted }} · Google {{ app.google_mrr_formatted }}`
- optionally show total MRR once, smaller, next to the name

{% endfor %}

Rules:
- fill MUST be solid pure {{ icon_marker_hex }} — no gradients, no borders, no glow, no texture
- do not use {{ icon_marker_hex }} / pure red anywhere else on the banner
- do not draw real logos, glyphs, photos, or text inside the markers
- place markers in a horizontal row under the apps label (or a tidy vertical list if space is tight)
- render every Apple / Google MRR value exactly as supplied — do not invent or omit a store
- markers should be roughly 40–56 px on the final {{ banner.width }}×{{ banner.height }} canvas (scale proportionally)
{% endif %}

### Right Status Card

Show:
- current period
- current MRR
- optional target progress

Use a dark rounded card with subtle borders and glow.

### Bottom Progress Bar

Draw a single wide horizontal progress bar as the main bottom graphic (full width within safe margins).

Style:
- capsule / pill track with rounded ends
- dark translucent track, subtle border
- filled portion with a smooth gradient (accent → primary / chart color)
- circular marker (thumb) on the fill edge with a thin light outline
- optional subtle tick marks along the track

Labels (exactly):
- left, below the track: {{ challenge.start_mrr_formatted }}
- right, below the track: {{ challenge.target_mrr_formatted }}

Position the fill and marker at:

{{ revenue.target_progress_percent }}%

Do not draw a revenue history chart, stacked bars, line chart, or legend.

## Accuracy Rules

This banner contains real financial data.

Therefore:

- Render every supplied number accurately.
- Do not invent revenue values.
- Do not change app names.
- Do not change dates.
- Do not change currencies.
- Do not round values unless the formatted value already contains rounding.
- Do not substitute placeholder values.
- Do not display Jinja variables or braces.
- Do not add additional statistics that were not supplied.
- Do not invent a revenue history chart.

## Final Output

Generate one complete polished X profile banner.

Leave the pure-red icon markers as specified. Real logos replace those markers afterward.
All other content must be publication-ready with no further overlays required.
